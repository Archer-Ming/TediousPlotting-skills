---
name: 专业绘图技能
description: 专业的科研绘图技能，包含箱线图，柱状图等各种常见的图形可视化绘制。用户通过提供数据，坐标标题，大标题等需求，你来完成准确的绘图并呈现给用户，本技能严格遵循固定的科研配色与字体规范,产出符合期刊投稿质量的可视化图表。
---
---

### 重要规则！

* 绘图风格技巧都在参考模板的代码中，请严格遵守绘图前提设置以及模板的绘图风格，按不同用户需求绘制科研级别的图表。
* 对于用户提供的数据中名称，按具体情况进行修正。比如：中文就对应SimHei等，确保生成的图不要有乱码，可视化位置正确；如果名称带有单位且显示不出来，把单位去除以确保图表不要出现乱码；如果数据名称为英语则使用TimesNe wRoman。
* 如果可视化的图不符合预期，请自检并重新绘制，直到达到预期。
* 模板代码提供的只是给你的模仿参考，里面的x坐标，y坐标数据，亦或是对数据的处理读取，标题名称，各种不同类型数据，请忽略。你要做的是学习模板的绘图逻辑，绘图风格，功能亮点。然后根据用户提供数据和要求绘制相似或者相同的模板可视化配图。
* 有些模板里面是多种类型图的复合，你学习完后必须按照用户需求选择性绘制，抑或是全部使用，具体情况具体考虑。
* 对于用户提及的可视化需求配图并不在模板里面的，你要按照在我这些模板中学习到的绘图风格技能，模仿并自主绘制适合用户需求的可视化配图。

### 前提

⚠️ 用户没明说图表类型时,先简短确认再画。

首先你得先确保绘图配置，确保符合科研级别，包含至少如下：

```
import matplotlib.pyplot as plt

# 字体:如果用户提供的数据有中文，则'SimHei'优先 。
# 如果没有则'Times New Roman'优先。
# plt.rcParams['font.family'] = 'serif'
# plt.rcParams['font.serif'] = ['SimHei','Times New Roman', 'Liberation Serif', # 'DejaVu Serif']

# 期刊配置:
plt.rcParams.update({
    'font.size': 14,
    'axes.labelsize': 18,
    'axes.titlesize': 16,
    'xtick.labelsize': 16,
    'ytick.labelsize': 16,
    'legend.fontsize': 12,
    'figure.dpi': 400,
    'figure.figsize': (10, 6),
    'axes.linewidth':1.2,
    'xtick.major.width': 1.2,
    'ytick.major.width': 1.2,
    'xtick.major.size': 6,
    'ytick.major.size': 6,
    'xtick.minor.size': 3,
    'ytick.minor.size': 3,
    'axes.spines.top': True,
    'axes.spines.right': True,
    'axes.unicode_minus': False
})
```

科研常见的可视化配图颜色大致如下，根据实际情况在下列选取，不要自己乱用配色：

```
colors = ['#2E86AB', '#A23B72', '#F18F01', '#C73E1D',"#649fca", "#c68f5f", "#63bd63", "#D05E5E", "#8F51C8"]
```

---

### 参考模板 Python

|             参考文件             | 何时需要                                                 |
| :------------------------------: | -------------------------------------------------------- |
|    reference\bar0line0box.md    | 当用户需要绘制柱状图，折线图，箱线图以及复合图表         |
| reference\scatter0violin0heat.md | 当用户需要绘制散点图，小提琴图，热力图以及变体，复合图表 |

### 输入格式

用户会以各种形式提供数据（可能直接文本输入，excel表格类型数据，图表类型数据？），标题（xy坐标，大标题，文本框？）等需求给你，结构至少包含如下：

* 数据
* x标题，y标题, 大标题
* 可能的其他美化需求

例如：

`帮我绘制一个柱状图，数据如下表格，其中x坐标为特征类型，y坐标为表格样本数量，大标题为“各类特征的样本数量统计”，添加图例。`

### 输出格式

你的输出是呈现一张符合用户需求格式为.png/.svg类型的图表。

### 出图前的自检！！！

每次画完图,在保存前过一遍:

- [ ] **字体无乱码**:如果数据出现中文标签时请确保使用SimHei字体优先。
- [ ] **标签不重叠**:x 轴主标签 vs 副标签、柱顶数值 vs 图例边界、tick label 旋转角度合适?
- [ ] **色阶适配数据**:热力图的 vmin/vmax 是否覆盖实际数据范围又不过宽?
- [ ] **y 轴留白足够**:柱顶数值标签上方是否预留 ≥15% 空间?(`ax.set_ylim(0, ymax * 1.18)`)
- [ ] **图例不遮挡数据**:`loc` 选对位置,必要时用 `bbox_to_anchor` 外置，不要导致遮挡。
- [ ] **数值精度合理**:整数计数用 `{int(h)}`,百分比 `.2f`,科学量 `.3e`
- [ ] **配色取自调色板**:没有用 matplotlib 默认色或随手挑的颜色
- [ ] **图幅与子图数量匹配**:子图越多 figsize 越大
- [ ] **大标题**:用户没要求就**不加**(很多用户用图嵌入论文已有 caption)
- [ ] 用户数据适配性：画的图类型是否和数据相契合？是否能有效呈现数据？

如果检查不通过,**自己 debug 重画**,不要把瑕疵图给用户！
